# Frontend - App

Parte frontend mobile do projeto, desenvolvida com _React Native_ e _Expo_.

A conexão com o backend ocorre com o uso de APIs.

## Desenvolvimento

Instalação de dependências: `npm install`.

## Execução

Iniciar o server: `npm start`.

O app pode ser visto no navegador.

É necessário que o backend esteja sendo executado.
